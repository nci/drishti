{
    "script" : "interpolation.py",
    "arguments" : {
	"nbrw" : ["INT", "Number of random walks starting at each pre-segmented pixel", 10],
	"sorw" : ["INT", "Steps of a random walk", 4000],
	"compression" : [ "BOOL", "Compress segmentation results", "True"],
	"allaxis" : ["BOOL", "If pre-segmentation is not exlusively in xy-plane", "False"],
	"denoise" : ["BOOL", "Smooth/denoise image data before processing", "False"],
	"uncertainty" : ["BOOL", "Return uncertainty of segmentation result", "False"],
	"ignore" : ["STR", "Ignore specific label(s), e.g. 2,5,6", "None"],
	"only" : ["STR", "Segment only specific label(s), e.g. 1,3,5", "all"],
	"smooth" : ["INT", "Number of smoothing iterations for segmentation result", 0],
	"platform" : ["STR", "One of none(this will auto detect), cuda, opencl_NVIDIA_GPU, opencl_Intel_CPU", "None"],
	"return_hits" : ["BOOL", "Return hits from each label. Only works for small image data", "False"],
	"acwe" : ["BOOL", "Post-processing with active contour", "False"],
	"acwe_alpha" : ["FLOAT", "Pushing force of active contour", 1.0],
	"acwe_smooth" : ["INT", "Smoothing steps of active contour ", 1],
	"acwe_steps" : ["INT", "Iterations of active contour", 3],
        "clean" : ["FLOAT", "Remove outliers, e.g. 0.5 means that objects smaller than 50 percent of the size of the largest object will be removed", 0.0],
	"fill" : ["FLOAT", "Fill holes, e.g. 0.5 means that all holes smaller than 50 percent of the entire label will be filled", 0.0]
    },
    "doc" :
"This script uses Biomedisa tool from https://biomedisa.info/
Works only on 3D data.

Available functions for 3D processing :
    process_volume

Modules Required : biomedisa, torch, torchvision, scikit_image,
                   medpy, simpleitkscipy, numba, importlib_metadata
                   mpi4py, keras, pycuda/pyopencl (depending on your gpu)

To install biomedisa on Windows -
Have a look at https://biomedisa.info/
https://github.com/biomedisa/biomedisa/blob/master/README/windows.md

Install Cuda (for nvidia gpu) and OpenCL (for other gpu)
If you don't have Visual Studio - Install visual studio cpp build tools - https://visualstudio.microsoft.com/visual-cpp-build-tools/
Install msmpisetup.exe from https://learn.microsoft.com/en-us/message-passing-interface/microsoft-mpi

uv pip install numba
uv pip install zarr
uv pip install numpy
uv pip install pycuda (for nvidia)  pyopencl (otherwise)
uv pip install mpi4py
uv Install pytorch from https://pytorch.org/
uv pip install keras
uv pip install scikit_image
uv pip install medpy
uv pip install importlib_metadata
uv pip install opencv-python
uv pip install matplotlib
uv pip install biomedisa

Install Segment Anything Model (SAM) for instance segmentation
uv pip install git+https://github.com/facebookresearch/segment-anything.git"
}
